enum PaymentType {
  SimplePayment,
  RecurringBilling,
}

enum RecurringPaymentType { subscription, tokenization }

enum FrequencyCyclePeriod {
  Monthly,
  Quaterly,
  Biannually,
  Annual,
}
